#ifndef LISTS_H
#define LISTS_H

#include <stdlib.h>

def islower(c):
def uppercase(str):
def print_last_digit(number):
def add(a, b):
def pow(a, b):
def fizzbuzz():
listint_t *insert_node(listint_t **head, int number);

#endif
